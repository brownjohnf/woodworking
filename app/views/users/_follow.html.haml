= form_for current_user.relationships.build(:followed_id => @user.id), :remote => true do |f|
  = f.hidden_field :followed_id
  .actions
    = f.submit "Add this person as an associate", :class => 'btn btn-success'
